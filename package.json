{
  "name": "enterprise-agent-os",
  "version": "1.0.0",
  "private": true,
  "description": "Self-hosted AI agent platform for enterprises — combining Paperclip orchestration, Hermes-style agents, and Downy-inspired security",
  "workspaces": [
    "packages/shared",
    "packages/agent-runtime",
    "packages/security-layer",
    "packages/mcp-hub",
    "packages/dashboard"
  ],
  "scripts": {
    "build": "npm run build -w @enterprise/shared && npm run build -w @enterprise/agent-runtime && npm run build -w @enterprise/security-layer && npm run build -w @enterprise/mcp-hub && npm run build -w @enterprise/dashboard",
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck -w @enterprise/shared --if-present && npm run typecheck -w @enterprise/agent-runtime --if-present && npm run typecheck -w @enterprise/security-layer --if-present && npm run typecheck -w @enterprise/mcp-hub --if-present && npm run typecheck -w @enterprise/dashboard --if-present",
    "dev": "echo 'Run individual packages with: npm run dev -w @enterprise/<package>'",
    "docker:up": "docker compose -f docker/docker-compose.yml up -d",
    "docker:up:gpu": "docker compose -f docker/docker-compose.yml --profile gpu up -d",
    "docker:down": "docker compose -f docker/docker-compose.yml down",
    "docker:logs": "docker compose -f docker/docker-compose.yml logs -f",
    "docker:build": "docker compose -f docker/docker-compose.yml build",
    "db:init": "docker compose -f docker/docker-compose.yml exec postgres psql -U enterprise -d enterprise_agents -f /docker-entrypoint-initdb.d/init-db.sql"
  }
}
